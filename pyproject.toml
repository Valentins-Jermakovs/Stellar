[project]
name = "stellar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "valentins jermakovs", email = "jermakovsvalentins@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "flask[async]>=3.1.3",
    "httpx>=0.28.1",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
stellar = "stellar:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
